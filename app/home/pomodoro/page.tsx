import { PomodoroTimer } from "@/components/home/pomodoro/pomodoro-timer";

export default function PomodoroPage() {
  return (
    <div className="flex flex-1 items-center justify-center min-h-[60vh]">
      <PomodoroTimer />
    </div>
  );
}
